info:
  name: UpdateTodo
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/todos/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {

       
        "status": "in-progress"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
