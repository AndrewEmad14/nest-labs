info:
  name: CreateTodo
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/todos"
  body:
    type: json
    data: |-
      {
        
        "task":"no",
        "status":"todo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
